{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "WeTalk 主窗口默认能力（前端事件 emit / 原生文件对话框 / 基础 core 权限）",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
